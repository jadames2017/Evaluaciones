let decimal = 8;

let decimal_Binario = decimal.toString(2);

console.log(`${decimal_Binario}`);

let binario = 1000;

let binario_decimal = binario.toString(2);

console.log(`${binario_decimal}`);
/**/
let decimal_1 = 950;

let decimal_Binario_1 = decimal_1.toString(2);

console.log(`${decimal_Binario_1}`);

let binario_1 = 9.5;

let binario_decimal_1 = binario_1.toString(10);

console.log(`${binario_decimal_1}`);
